global:
  scrape_interval: 60s       # 与 Agent 心跳周期一致
  evaluation_interval: 60s

scrape_configs:
  - job_name: mxsec-agentcenter
    static_configs:
      - targets:
          - agentcenter:8080   # AgentCenter HTTP 管理端口（/metrics 端点）
