# 已确认、暂无修复版本可升的依赖通告。
#
# 每条必须写明：为什么现在升不了、什么条件下应当移除。
# CI 只放行这里列出的编号；出现清单外的通告会失败，
# 清单里的条目若已从扫描结果消失，也会失败——那说明上游已修，
# 应当升级依赖并删掉对应行，而不是留着一条永远不会命中的豁免。

# golang.org/x/crypto —— 上游尚未发布修复版本（govulncheck 报 Fixed in: N/A）。
# 复查条件：x/crypto 发布含修复的版本后升级依赖并移除本行。
GO-2026-5932
